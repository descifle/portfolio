import React from 'react';
// state = {}



const SearchBar = () => {

    const handleClick = (event) => {
        
     }

     return (
        <div className="ui three item menu mt-4">
            <a onClick={this.handleClick} href="#something" className="item">Apps</a>
            <a onClick={this.handleClick} href="#something1" className="item">Websites</a>
            <a onClick={this.handleClick} href="#something2" className="item">Emails</a>
        </div> 
    )
}

export default SearchBar;